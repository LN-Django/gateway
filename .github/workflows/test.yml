name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.x' 
      - run: python ./boilerplate/manage.py test
      - run: echo "Printing env variable"
      - run: echo ${{ secrets.APP_NAME }}